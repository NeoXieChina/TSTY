import 'package:flutter/material.dart';

class ParentEntryPage extends StatelessWidget {
  const ParentEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('家长入口')),
      body: const Center(child: Text('功能开发中')),
    );
  }
}
